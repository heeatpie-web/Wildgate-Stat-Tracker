# Specification (Alias)

This file exists because some workflows/tools expect `.codemachine/inputs/specification.md`.

The canonical file in this repo is:
- `.codemachine/inputs/specifications.md`

If you edit one, consider keeping the other in sync.

Operational note:
- Run `powershell -File ./codemachine-fixed.ps1 doctor` before each new codemachine session.

